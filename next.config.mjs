/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["untitled-twkmuar27a-uc.a.run.app"], // Add the domain here
  },
};

export default nextConfig;
